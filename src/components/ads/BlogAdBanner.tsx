"use client";

import { ContentAdBanner } from "@/components/ads/ContentAdBanner";

/** In-flow banner below blog header (not above site chrome). */
export function BlogAdBanner() {
  return <ContentAdBanner region="blog-banner" />;
}
